interface BuildCommand {
  command: string;
  output?: string;
}

interface EnvTemplate {
  [key: string]: string | undefined;
}

interface RedirectRule {
  source: string;
  has: { status: number; location: string };
}

interface HeaderPolicy {
  source: string;
  headers: Array<{ key: string; value: string }>;
}

interface CacheStrategy {
  src: string;
  locales?: boolean;
  revalidate: number | 'ptivate' | 'public';
}

interface FrameworkSettings {
  vercel?: { [key: string]: any };
  netlify?: { [key: string]: any };
}

interface DeployConfig {
  build: BuildCommand;
  env: EnvTemplate;
  redirects: Array<RedirectRule>;
  headers: Array<HeaderPolicy>;
  cacheStrategies: Array<CacheStrategy>;
  frameworkSettings: FrameworkSettings;
}

export const deployConfig: DeployConfig = {
  build: {
    command: 'next build',
    output: '.next'
  },
  env: {
    NEXT_PUBLIC_API_URL: process.env.NEXT_PUBLIC_API_URL,
    NEXT_PRIVATE_KEY: process.env.NEXT_PRIVATE_KEY
  },
  redirects: [
    {
      source: '/old-url',
      has: { status: 301, location: '/new-url' }
    }
  ],
  headers: [
    {
      source: '/',
      headers: [
        { key: 'strict-transport-security', value: 'max-age=63072000; includeSubDomains; preload' },
        { key: 'x-content-type-options', value: 'nosniff' }
      ]
    }
  ],
  cacheStrategies: [
    {
      src: '/api/:path*',
      revalidate: 'private'
    },
    {
      src: '/',
      locales: true,
      revalidate: 60
    }
  ],
  frameworkSettings: {
    vercel: { buildCommand: 'next build', startCommand: 'next start' },
    netlify: { functions: 'functions/' }
  }
};

interface BuildCommand {
  command: string;
  output?: string;
}

interface EnvTemplate {
  [key: string]: string | undefined;
}

interface RedirectRule {
  source: string;
  has: { status: number; location: string };
}

interface HeaderPolicy {
  source: string;
  headers: Array<{ key: string; value: string }>;
}

interface CacheStrategy {
  src: string;
  locales?: boolean;
  revalidate: number | 'ptivate' | 'public';
}

interface FrameworkSettings {
  vercel?: { [key: string]: any };
  netlify?: { [key: string]: any };
}

interface DeployConfig {
  build: BuildCommand;
  env: EnvTemplate;
  redirects: Array<RedirectRule>;
  headers: Array<HeaderPolicy>;
  cacheStrategies: Array<CacheStrategy>;
  frameworkSettings: FrameworkSettings;
}

export const deployConfig: DeployConfig = {
  build: {
    command: 'next build',
    output: '.next'
  },
  env: {
    NEXT_PUBLIC_API_URL: process.env.NEXT_PUBLIC_API_URL,
    NEXT_PRIVATE_KEY: process.env.NEXT_PRIVATE_KEY
  },
  redirects: [
    {
      source: '/old-url',
      has: { status: 301, location: '/new-url' }
    }
  ],
  headers: [
    {
      source: '/',
      headers: [
        { key: 'strict-transport-security', value: 'max-age=63072000; includeSubDomains; preload' },
        { key: 'x-content-type-options', value: 'nosniff' }
      ]
    }
  ],
  cacheStrategies: [
    {
      src: '/api/:path*',
      revalidate: 'private'
    },
    {
      src: '/',
      locales: true,
      revalidate: 60
    }
  ],
  frameworkSettings: {
    vercel: { buildCommand: 'next build', startCommand: 'next start' },
    netlify: { functions: 'functions/' }
  }
};